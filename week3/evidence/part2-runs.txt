week3 part 2 - the four contract ramps

Binary: cargo install --path . --quiet (release build, already installed at
~/.cargo/bin/ising). All four commands ran in parallel on 2026-09-16, one
process per ramp, from week3/, writing into the git-ignored week3/artifacts/.

command, wall clock, measured rows

coarse-l32, 2.80 s, 105000 rows
  ising --update metropolis --l 32 --t-from 1.5 --t-to 3.5 --t-step 0.1 \
    --discard 2000 --measure 5000 --seed 1042 --out artifacts/coarse-l32

coarse-l64, 10.54 s, 105000 rows
  ising --update metropolis --l 64 --t-from 1.5 --t-to 3.5 --t-step 0.1 \
    --discard 2000 --measure 5000 --seed 42 --out artifacts/coarse-l64

window-l32, 22.25 s, 1300000 rows
  ising --update metropolis --l 32 --t-from 2.0 --t-to 2.6 --t-step 0.05 \
    --discard 2000 --measure 100000 --seed 1042 --out artifacts/window-l32

window-l64, 84.59 s, 1300000 rows
  ising --update metropolis --l 64 --t-from 2.0 --t-to 2.6 --t-step 0.05 \
    --discard 2000 --measure 100000 --seed 42 --out artifacts/window-l64

Wall clock of the whole batch, run in parallel: 84.63 s. Row counts match the
contract exactly: 21 temperatures at 5000 measured sweeps in each coarse
folder, 13 temperatures at 100000 in each window folder, 2810000 rows and
about 160 MB in total. Each folder holds run.json and series.jsonl.

Analysis, all reading those folders and nothing else:

  .venv/bin/python scripts/peaks.py                 -> evidence/peaks.txt
  .venv/bin/python scripts/plot_magnetization.py    -> evidence/magnetization.png
  .venv/bin/python scripts/plot_susceptibility.py   -> evidence/susceptibility.png

The tests for the analysis module are self-contained:

  .venv/bin/python -m unittest scripts.test_peaks
