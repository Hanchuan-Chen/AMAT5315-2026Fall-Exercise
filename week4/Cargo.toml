[package]
name = "week4"
version = "0.1.0"
edition = "2024"
description = "Advance the two-dimensional incompressible vorticity equation"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
debug = 1
