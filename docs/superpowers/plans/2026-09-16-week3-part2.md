# Week 3 Part 2 Analysis Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce Part 2 of the week-3 sheet — the four contract ramps, the susceptibility peak estimate of `T_c` from two lattice sizes, the magnetization and susceptibility charts, and the raw numbers that back all three — without touching the sampler's command-line contract.

**Architecture:** No Rust changes. Four `ising` invocations write `week3/artifacts/<name>/{run.json,series.jsonl}`; one Python analysis module, `week3/scripts/peaks.py`, owns row reading, the Equation 10 susceptibility, the five-point parabola peak fit, Equation 11, and the `peaks.txt` report; the two chart scripts import that module and draw `evidence/magnetization.png` and `evidence/susceptibility.png` with matplotlib.

**Tech Stack:** the already-installed release binary `ising` (Rust 1.98.1), Python 3.12 from `week3/.venv` (gitignored, `uv pip install numpy matplotlib`), stdlib `unittest` for the analysis tests, matplotlib `Agg`.

**Spec:** docs/superpowers/specs/2026-09-16-week3-part2-design.md

## Global Constraints

- Work only in `/Users/eureka/Documents/Codex/2026-09-02/ban/AMAT5315-2026Fall-Exercise` on `main`; check `git status` before every commit.
- Never amend, squash, rebase, reset, or force-push; never push to `origin` in this phase. Local commits only.
- Chronological discipline: design doc, then plan doc, then RED test, then GREEN implementation, then evidence, each as its own conventional commit.
- `week3/artifacts/`, `week3/runs/`, `week3/.venv/` stay out of git; the four folders hold about 160 MB of rows.
- The four ramps use the sheet's exact values, verbatim, with `cargo install --path . --quiet` first.
- `chi(T) = L^2 (<m^2> - <|m|>^2) / T` is defined once, in `peaks.py`, and imported by every consumer.
- `T_c = 2 * T_peak(64) - T_peak(32)`, with `T_peak` the vertex of a parabola through the five grid points around the largest `chi` of that size.
- Every committed file stays below 5 MB; every reported number comes from a command that actually ran in this session.

## File map

- Create `docs/superpowers/specs/2026-09-16-week3-part2-design.md`: this phase's design, including the uncertainties section.
- Create `docs/superpowers/plans/2026-09-16-week3-part2.md`: this plan.
- Create `week3/scripts/peaks.py`: row reading, susceptibility, peak fit, extrapolation, `peaks.txt`.
- Create `week3/scripts/test_peaks.py`: stdlib `unittest` RED tests on a synthetic run folder.
- Create `week3/scripts/plot_magnetization.py`: `evidence/magnetization.png`.
- Create `week3/scripts/plot_susceptibility.py`: `evidence/susceptibility.png`.
- Create `week3/artifact runs` (not committed): `week3/artifacts/{coarse-l32,coarse-l64,window-l32,window-l64}/`.
- Create `week3/evidence/peaks.txt`, `week3/evidence/magnetization.png`, `week3/evidence/susceptibility.png`.

## Task 1: Design doc (commit)

**Files:** create `docs/superpowers/specs/2026-09-16-week3-part2-design.md`.

- [ ] Record the goal, the scope boundary against Part 3/4, the four command lines, the folder shapes, the Equation 10 definition and why the response form is not used, the merge rule for the two grids, the five-point parabola fit, Equation 11, the two charts, the uncertainties section (sampling error, equilibration, peak-fit bias, finite-size extrapolation, grid discretization), and the risks.
- [ ] Commit `docs: design the week 3 part 2 analysis`.

## Task 2: Plan doc (commit)

**Files:** create `docs/superpowers/plans/2026-09-16-week3-part2.md`.

- [ ] Write this plan.
- [ ] Commit `docs: plan the week 3 part 2 analysis`.

## Task 3: RED test for the analysis (commit)

**Files:** create `week3/scripts/test_peaks.py`.

- [ ] Write a stdlib `unittest` suite that fails against a missing `peaks.py` and, once it exists, pins the contract:
  - a synthetic run folder in a temporary directory (five temperatures, `L = 8`, hand-built `M` columns) whose `<|m|>`, `<m^2>` and `chi` are known in closed form;
  - `read_run` returns the per-temperature means in ramp order;
  - `chi_series` reproduces `L^2 (<m^2> - <|m|>^2) / T` on that fixture, including the sign of the correction;
  - `fit_peak` recovers the exact vertex of an exactly quadratic five-point sample to `1e-9`;
  - `fit_peak` moves with the data (a parabola whose vertex sits at a named grid offset is recovered at that offset, not at the grid point);
  - `onsager_abs_m` returns `0` at and above `T_c`, is monotone increasing downward in `T`, equals `(1 - sinh(2/T)^-4)^(1/8)` below `T_c`, and reproduces the sheet's `T = 1.8` value `0.9569` and `T = 1.5` value `0.9866` to four decimals;
  - `merge_grid` prefers the window rows inside `[2.0, 2.6]` and the coarse rows outside, and yields 27 temperatures for the sheet's two grids.
- [ ] Run `.venv/bin/python -m unittest scripts.test_peaks` inside `week3/` and record the import/attribute failures.
- [ ] Commit `test: specify the week 3 part 2 susceptibility analysis`.

## Task 4: GREEN analysis (commit)

**Files:** create `week3/scripts/peaks.py`.

- [ ] `read_run(folder)` → `(L, [(T, mean_abs_m, mean_m_sq, n), ...])` in ascending temperature order, streaming `series.jsonl` line by line.
- [ ] `chi_series(rows)` → `L^2 (<m^2> - <|m|>^2) / T` per temperature.
- [ ] `merge_grid(coarse_rows, window_rows)` → the sheet's 27-temperature grid, window rows winning inside `[2.0, 2.6]`, coarse rows elsewhere, keyed on the exact `T` value the contract prints.
- [ ] `fit_peak(ts, chis)` → the vertex of the least-squares parabola through the five points centred on the largest `chi`, fitted on `T - T_v` for conditioning; raise a clear error when fewer than five points exist.
- [ ] `onsager_abs_m(t)` → Equation 3 with `T_c = 2 / ln(1 + sqrt 2)`.
- [ ] CLI: read the four artifact folders, print the cold `mean |M|` per size, the two fitted peaks with their grid maxima, `T_c = 2 T_peak(64) - T_peak(32)`, the deviation from `2.26919` in percent, and write the identical text to `evidence/peaks.txt`.
- [ ] `.venv/bin/python -m unittest scripts.test_peaks` passes.
- [ ] Commit `feat: fit the susceptibility peaks and extrapolate the critical temperature`.

## Task 5: GREEN charts (commit)

**Files:** create `week3/scripts/plot_magnetization.py`, `week3/scripts/plot_susceptibility.py`.

- [ ] `plot_magnetization.py`: mean `|m|` against `T` for `L = 64` on the merged grid, Onsager's curve for `T < T_c`, dashed vertical line at `T_c = 2.26919`, axis labels and a legend; save `evidence/magnetization.png`.
- [ ] `plot_susceptibility.py`: `chi(T)` for both sizes on the merged grids, dashed line at `T_c`, dotted lines at the two fitted peaks printed in the legend with their values; save `evidence/susceptibility.png`.
- [ ] Both scripts import `peaks` for the data path and the susceptibility; neither re-derives physics.
- [ ] Commit `feat: draw the magnetization and susceptibility evidence charts`.

## Task 6: Evidence and verification (commit)

**Files:** create `week3/evidence/peaks.txt`, `week3/evidence/magnetization.png`, `week3/evidence/susceptibility.png`.

- [ ] Confirm all four artifact folders hold `run.json` and `series.jsonl`, and check the per-folder row counts against the contract: 105000 rows in each coarse folder (21 temperatures), 1300000 in each window folder (13 temperatures).
- [ ] Record the four commands with their wall-clock times and the row counts in the report, for the later README.
- [ ] Run `scripts/peaks.py`; save and read `evidence/peaks.txt`.
- [ ] Run both chart scripts; open the two PNGs and check them against the sheet's reference shapes.
- [ ] Verify every Part 2 pass condition: four complete folders; `L = 64` within about `0.1` of the reference at `T = 2.25, 2.3, 2.6`; `mean |m| >= 0.9` at `T = 1.5` for each size and `< 0.06` at `T = 3.0` for `L = 64`; both fitted peaks in `[2.30, 2.40]` with the `L = 64` peak taller, narrower and lower; `|T_c - 2.26919| / 2.26919 < 0.02`.
- [ ] If a condition fails, investigate the cause (temperature scale, energy convention, sample count) rather than moving the threshold.
- [ ] Commit `docs: add the week 3 part 2 critical-temperature evidence`.

## Self-check before reporting

- [ ] `git log --oneline` shows design, plan, RED test, two GREEN commits and evidence, in that order, none pushed.
- [ ] `git status --short` shows no artifact or virtualenv path staged.
- [ ] Every committed file below 5 MB.
- [ ] The reported numbers are copied from this session's command output.
